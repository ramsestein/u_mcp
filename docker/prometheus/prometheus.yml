"""Prometheus configuration."""

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "umcp-gateway"
    static_configs:
      - targets: ["gateway:8000"]
    metrics_path: "/metrics"